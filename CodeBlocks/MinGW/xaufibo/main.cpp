#include <iostream>

using namespace std;

void solve(int n,int k)
{
    if(n%2==0) f[1]=1;
    else f[2]=1;
    if(n==2) return cout << 0,void();
    int tmp=n;
    if()
    while(tmp>0)
    {
        tmp=(n/13);
    }


    cout << f[k];
}
int main()
{
    cin >> T;
    while(T--)
    {
        cin >> n >> k;
        n++;
        solve(n,k);
    }
    return 0;
}
